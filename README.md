# Macro-string-to-decimal
Calculating string to decimal using macros in RISC architecture 
This project implements a set of reusable macros designed for string processing.
Deals with register managment, memory handling
